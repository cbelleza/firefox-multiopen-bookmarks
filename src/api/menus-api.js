/**
 * Reserved for future browser.menus integration (not used in popup/sidebar).
 * Kept as stub to avoid breaking imports if re-added.
 * @deprecated
 */
export async function createMenus(_definitions) {
  return Promise.resolve();
}
